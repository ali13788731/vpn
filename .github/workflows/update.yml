name: V2Ray Collector

on:
  schedule:
    # هر 45 دقیقه یکبار اجرا شود (30 دقیقه کمی ریسکی است برای تلگرام)
    # فرمت: دقیقه ساعت روز ماه هفته
    - cron: '*/45 * * * *'
  workflow_dispatch:

# جلوگیری از اجرای همزمان چندین اکشن برای جلوگیری از تداخل در گیت
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    # اگر اسکریپت گیر کرد، بعد از ۱۵ دقیقه خودکار قطع شود تا سهمیه اکانت تمام نشود
    timeout-minutes: 15 

    steps:
    - name: Checkout code
      uses: actions/checkout@v4 # نسخه جدیدتر
      with:
        fetch-depth: 0 # برای پوش کردن تاریخچه کامل لازم است

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip' # کش کردن پکیج‌ها برای سرعت بیشتر

    - name: Install dependencies
      # اگر فایل requirements.txt دارید از آن استفاده کنید، اگر نه همین روش خوب است
      # پکیج tqdm را هم اضافه کردم چون در کد پایتون پیشنهاد دادم
      run: |
        pip install --upgrade pip
        pip install telethon python-socks[asyncio] tqdm

    - name: Run collector script
      env:
        SESSION_STRING: ${{ secrets.SESSION_STRING }}
        API_ID: ${{ secrets.API_ID }}
        API_HASH: ${{ secrets.API_HASH }}
      run: python collector.py

    - name: Commit and Push
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        
        # اضافه کردن فایل
        git add sub.txt sub_raw.txt
        
        # بررسی وضعیت تغییرات
        if [ -n "$(git status --porcelain)" ]; then
          echo "Changes detected. Committing..."
          git commit -m "🚀 Update configs [$(date '+%Y-%m-%d %H:%M:%S')]"
          
          # ترفند جلوگیری از خطا: دریافت آخرین تغییرات قبل از پوش
          git pull --rebase origin main
          git push origin main
        else
          echo "No changes to commit. Skipping push."
        fi
