name: V2Ray Collector

on:
  schedule:
    # اجرا هر 45 دقیقه (ایمن تر از 30 دقیقه)
    - cron: '*/45 * * * *'
  workflow_dispatch:

# جلوگیری از تداخل اجراهای همزمان
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    # اگر بیشتر از 15 دقیقه طول کشید، قطع کن تا سهمیه نسوزد
    timeout-minutes: 15

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        # کش را حذف کردیم تا ارور ندهد

    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install telethon python-socks[asyncio] tqdm

    - name: Run collector script
      env:
        SESSION_STRING: ${{ secrets.SESSION_STRING }}
        API_ID: ${{ secrets.API_ID }}
        API_HASH: ${{ secrets.API_HASH }}
      run: python collector.py

    - name: Commit and Push
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        
        # فایل‌هایی که می‌خواهید ذخیره شوند
        git add sub.txt sub_raw.txt
        
        # بررسی تغییرات
        if [ -n "$(git status --porcelain)" ]; then
          echo "Changes detected. Committing..."
          git commit -m "🚀 Update configs [$(date '+%Y-%m-%d %H:%M:%S')]"
          
          # دریافت آخرین تغییرات قبل از پوش برای جلوگیری از خطا
          git pull --rebase origin main
          git push origin main
        else
          echo "No changes to commit. Skipping push."
        fi
